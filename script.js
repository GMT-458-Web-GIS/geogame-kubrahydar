// Wait for the DOM (HTML structure) to be fully loaded before running any script
document.addEventListener('DOMContentLoaded', (event) => {
    
    // This is the entry point for our game.
    console.log('ISS Photo Mission script loaded!');
    console.log('Waiting for Step 2: Initialize Cesium...');

    // We will initialize the Cesium viewer and game logic here in the next steps.

});